# card-with-words-and-gaps

## Get Started

```shell
npm install
npm run compile:shared # compile the shared module
```

Start the UI on http://localhost:4200

```shell
npm run start:ui
```

Start the server on http://localhost:3000

```shell
npm run start:server
```